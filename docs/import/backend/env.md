### `docs/env.md`
```markdown
# Переменные окружения (Backend)

| Имя             | Где     | Описание                                   |
|-----------------|---------|--------------------------------------------|
| PORT            | Railway | Порт HTTP (по умолчанию 8080)              |
| ALLOWED_ORIGINS | Railway | CSV список доменов для CORS в production   |

Смотри сводную матрицу в gtrack-docs/ops/ENV_MATRIX.md
