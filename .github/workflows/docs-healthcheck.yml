name: Docs Healthcheck

on:
  workflow_dispatch:

jobs:
  healthcheck:
    runs-on: ubuntu-latest
    steps:
      - name: Check docs portal availability
        run: |
          curl --fail --silent --show-error --location --retry 3 --retry-all-errors https://docs.g-track.eu > /dev/null
